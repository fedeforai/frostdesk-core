# Draft events — regola di prodotto

Gli eventi draft vivono solo in **instructor_draft_events**. **conversation_events** è legacy e non va usata per KPI draft.

A livello prodotto è disponibile la VIEW **draft_events** che punta a `instructor_draft_events` (nome intuitivo senza toccare lo schema legacy).
